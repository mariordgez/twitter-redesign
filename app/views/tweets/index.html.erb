<section class="section">
  <div class="container">
    <div class="columns">
    <% if user_signed_in? %>
     <%= render 'my-profile' %>
     <% else %>
      <%= render 'trending' %>
       <% end %>
      <%= render 'tweets' %>
      <%= render 'who-to-follow' %>
    </div>
  </div>
</section>